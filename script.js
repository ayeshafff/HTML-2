document.getElementById('surveyForm').addEventListener('submit', function(e) {
    e.preventDefault();
    
    // Add desired actions on form submission, e.g. sending the form data to a server
    alert('Form submitted!'); 
});
